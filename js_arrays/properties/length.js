// Sets or returns the number of elements in an array

const fruits = ["Banana", "Orange", "Apple", "Mango"];
let length =  fruits.length;

console.log(length)

// In other to set the array
 fruits.length = 2
 console.log(fruits)